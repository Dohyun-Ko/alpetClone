$(`#sidebarWinter`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `winter`);
    location.href = `goodsList.html`;
})

$(`#sidebarAll-in-one`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `all-in-one`);
    location.href = `goodsList.html`;
})

$(`#sidebarT-shirt`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `t-shirt`);
    location.href = `goodsList.html`;
})
$(`#sidebarHarness`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `harness`);
    location.href = `goodsList.html`;
})
$(`#sidebarsportsWear`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `sportsWear`);
    location.href = `goodsList.html`;
})
$(`#sidebarSeason`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `season`);
    location.href = `goodsList.html`;
})
$(`#sidebarOne-piece`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `one-piece`);
    location.href = `goodsList.html`;
})
$(`#sidebarSeol`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `seol`);
    location.href = `goodsList.html`;
})
$(`#sidebarPadding`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `padding`);
    location.href = `goodsList.html`;
})
$(`#sidebarEvent`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `event`);
    location.href = `goodsList.html`;
})
$(`#sidebarBrand`).click((event) => {
    event.preventDefault();
    localStorage.setItem(`category`, `brand`);
    location.href = `goodsList.html`;
})